<div id="banner">
  <%= render partial: 'shared/navbar' %>
  <div class="bg-light.bg-gradient ">
    <% if current_user.role == 'mentor' %>
      <%= render partial: 'mentor_dashboard' %>
    <% else %>
      <%= render partial: 'student_dashboard' %>
    </div>
    <% end %>
  </div>
</div>
